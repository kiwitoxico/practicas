<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Web con Barra Lateral</title>
    <style>
        /* Reset de estilos básicos */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            display: flex;
            background-color: #f4f6f9;
            color: #333;
        }

        /* Estilos de la Barra Lateral */
        .sidebar {
            width: 260px;
            height: 100vh;
            background-color: #1e293b;
            color: #fff;
            position: fixed;
            top: 0;
            left: 0;
            padding: 20px;
            display: flex;
            flex-direction: column;
        }

        .sidebar h2 {
            font-size: 1.5rem;
            margin-bottom: 30px;
            padding-bottom: 10px;
            border-bottom: 1px solid #334155;
            text-align: center;
            color: #38bdf8;
        }

        .sidebar-menu {
            list-style: none;
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        .sidebar-menu a {
            display: block;
            color: #cbd5e1;
            text-decoration: none;
            padding: 12px 15px;
            border-radius: 6px;
            transition: all 0.3s ease;
        }

        /* Efecto al pasar el ratón por las opciones */
        .sidebar-menu a:hover {
            background-color: #334155;
            color: #fff;
            padding-left: 20px;
        }

        /* Opción activa por defecto */
        .sidebar-menu a.active {
            background-color: #38bdf8;
            color: #1e293b;
            font-weight: bold;
        }

        /* Estilos del Contenido Principal */
        .main-content {
            margin-left: 260px; /* Desplaza el contenido para que no lo tape la barra lateral */
            padding: 40px;
            width: calc(100% - 260px);
        }

        .main-content h1 {
            font-size: 2rem;
            margin-bottom: 20px;
            color: #0f172a;
        }

        .main-content p {
            font-size: 1.1rem;
            line-height: 1.6;
            color: #475569;
        }
    </style>
</head>
<body>

    <aside class="sidebar">
        <h2>Mi Panel</h2>
        <ul class="sidebar-menu">
            <li><a href="#" class="active">🏠 Inicio</a></li>
            <li><a href="#">📦 Artículos</a></li>
            <li><a href="#">📊 Estadísticas</a></li>
            <li><a href="#">⚙️ Configuración</a></li>
            <li><a href="#">📞 Soporte</a></li>
        </ul>
    </aside>

    <main class="main-content">
        <h1>Bienvenido al sistema</h1>
        <p>Este es el contenedor principal de tu página web. Como la barra lateral está fija a la izquierda, puedes añadir aquí todo el texto, tablas, JSONs o componentes que necesites, y la página hará scroll de forma independiente sin mover el menú.</p>
    </main>

</body>
</html>